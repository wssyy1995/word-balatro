# 30 张新女巫牌设计 —— 「暗影契约」扩展包

> 本扩展包在现有 15 张女巫牌基础上，新增 **30 张** 女巫牌，并引入 **「组合技能（Synergy）」** 机制：当特定几张女巫牌同时装备时，会触发额外的联动效果。
>
> 设计原则：每张新牌都有独立的触发逻辑，同时预留 `comboGroup` 字段供后续 `calcWordScore` / `playHand` 中检测组合并追加效果。

---

## 一、女巫牌数据格式说明

```js
{
  name: '牌名',
  type: 'witch',
  scope: 'per_card' | 'whole_word' | 'limit' | 'flat_bonus',
  trigger: '触发条件标识',
  operation: 'add' | 'multi_adds_value' | 'multi_accumulation', // 可选
  value: 数值,
  cost: 商店价格,
  desc: '玩家可见描述',
  comboGroup: '组合派系标识', // 新增字段，用于组合技能检测
  comboWith: ['另一张牌名'],    // 可选：明确写出组合对象
  rarity: 'common' | 'rare' | 'epic' // 可选：影响出现概率
}
```

**组合技能实现建议**：
在 `js/game.js` 的 `calcWordScore()` 末尾或 `playHand()` 的计分阶段后，增加一段 `applySynergyEffects(cards, jokers, result)` 逻辑：遍历所有 `comboGroup` 非空的 joker，检测同组/指定组合是否同时存在，若存在则按组合规则修改 `result.mult` / `result.base`。

---

## 二、新增 30 张女巫牌详表

### 【派系 A：暗影字母】共 6 张
> 组合核心：利用单词中特定位置的字母（末字母、中间字母、对称位置）触发效果。

| # | 名称 | Scope | Trigger | Op | Value | Cost | 描述 | ComboGroup |
|---|------|-------|---------|----|-------|------|------|------------|
| 1 | **末字之刃** | per_card | `last_letter` | add | 40 | 5 | 单词最后一个字母分数 +40 | shadow_letter |
| 2 | **中段收割** | whole_word | `middle_vowel` | multi_adds_value | 4 | 7 | 单词含元音且元音不在首尾，倍率 +4 | shadow_letter |
| 3 | **镜像双子** | whole_word | `mirror_pair` | multi_adds_value | 6 | 8 | 单词存在对称位置相同字母（如AB**C**BA 的第3位），倍率 +6 | shadow_letter |
| 4 | **暗影X** | per_card | `letter_x` | — | 4 | 6 | 字母 X 的分数 ×4（与珍稀之力可叠加） | shadow_letter |
| 5 | **消元术** | whole_word | `no_duplicate` | multi_adds_value | 2 | 8 | 单词中所有字母均不重复，倍率 +2；若有重复，倍率 −1 | shadow_letter |
| 6 | **字母潮汐** | per_card | `even_position` | add | 25 | 5 | 偶数位置字母（2,4,6…）分数 +25 | shadow_letter |

---

### 【派系 B：赌徒之轮】共 6 张
> 组合核心：概率、随机、风险与回报。与「勇敢试错」「以小博大」形成赌徒体系。

| # | 名称 | Scope | Trigger | Op | Value | Cost | 描述 | ComboGroup |
|---|------|-------|---------|----|-------|------|------|------------|
| 7 | **命运硬币** | whole_word | `coin_flip` | — | 2 | 6 | 50% 概率倍率 ×2，50% 概率倍率不变 | gambler |
| 8 | **孤注一掷** | whole_word | `all_in` | multi_adds_value | 12 | 9 | 若本回合剩余出牌次数为 1 且手牌全出，倍率 +12 | gambler |
| 9 | **幸运7** | whole_word | `length_7` | — | 2.5 | 10 | 单词恰好 7 个字母，倍率 ×2.5 | gambler |
| 10 | **逆转翻盘** | flat_bonus | `discard_bonus` | — | 15 | 6 | 每次弃牌后，下次出牌基础分 +15（可叠加） | gambler |
| 11 | **赌注翻倍** | whole_word | `double_or_nothing` | — | 2 | 8 | 若上回合出牌倍率 <1.5，本回合倍率 ×2 | gambler |
| 12 | **庄家通吃** | whole_word | `face_card_rich` | multi_adds_value | 8 | 9 | 单词含 ≥2 张人头牌（J/Q/X/Y/Z），倍率 +8 | gambler |

---

### 【派系 C：自然共鸣】共 6 张
> 组合核心：元音、辅音、字母自然属性。与「元音强化」「元音为首」形成自然系。

| # | 名称 | Scope | Trigger | Op | Value | Cost | 描述 | ComboGroup |
|---|------|-------|---------|----|-------|------|------|------------|
| 13 | **辅音堡垒** | per_card | `is_consonant` | — | 2.5 | 6 | 辅音字母分数 ×2.5 | nature |
| 14 | **元音潮汐** | whole_word | `all_vowel` | multi_adds_value | 7 | 8 | 单词全部为元音字母（≥3 个），倍率 +7 | nature |
| 15 | **森林之息** | whole_word | `has_a_and_e` | multi_adds_value | 4 | 5 | 单词同时含 A 和 E，倍率 +4 | nature |
| 16 | **火山爆发** | per_card | `letter_t` | add | 35 | 5 | 字母 T 分数 +35 | nature |
| 17 | **冰川凝固** | whole_word | `length_8_plus` | — | 1.8 | 7 | 单词 ≥8 个字母，倍率 ×1.8 | nature |
| 18 | **风暴之眼** | limit | `storm_eye` | limit | 2 | 10 | 限 2 次：打出单词含 S-T-O-R-M 中任意 3 个不同字母，倍率 ×3 | nature |

---

### 【派系 D：时间旅者】共 6 张
> 组合核心：跨回合记忆、历史出牌影响当前。与「首字连击」「生命延续」形成时间系。

| # | 名称 | Scope | Trigger | Op | Value | Cost | 描述 | ComboGroup |
|---|------|-------|---------|----|-------|------|------|------------|
| 19 | **历史回响** | whole_word | `repeat_last_word` | multi_adds_value | 10 | 8 | 打出与上回合完全相同的单词，倍率 +10 | time |
| 20 | **时间倒流** | limit | `time_rewind` | limit | 1 | 12 | 限 1 次：出牌后若分数未达目标，可将该牌收回手牌（恢复出牌次数） | time |
| 21 | **加速未来** | whole_word | `haste_bonus` | multi_adds_value | 5 | 6 | 打出单词后剩余时间 <10 秒（争分夺秒期间），倍率 +5 | time |
| 22 | **遗忘咒** | flat_bonus | `forget_penalty` | — | 20 | 5 | 每次打出与上回合首字母不同的单词，基础分 +20 | time |
| 23 | **轮回印记** | whole_word | `same_length_streak` | multi_adds_value | 3 | 7 | 连续打出相同长度单词（≥3 次），每次倍率 +3 | time |
| 24 | **预言家** | per_card | `predicted_letter` | add | 50 | 8 | 回合开始时随机预言一个字母，打出该字母时分数 +50 | time |

---

### 【派系 E：炼金术士】共 6 张
> 组合核心：分数、金币、药水之间的资源转换。与商店系统产生联动。

| # | 名称 | Scope | Trigger | Op | Value | Cost | 描述 | ComboGroup |
|---|------|-------|---------|----|-------|------|------|------------|
| 25 | **点金术** | whole_word | `gold_word` | multi_adds_value | 3 | 7 | 打出单词得分 ≥100 时，倍率 +3，同时获得 1 金币 | alchemy |
| 26 | **等价交换** | flat_bonus | `exchange` | — | 30 | 6 | 每次出牌消耗 1 金币，基础分 +30（金币不足时不触发） | alchemy |
| 27 | **炼金强化** | per_card | `upgraded_card` | — | 3 | 8 | 已被药水升级过的字母分数 ×3 | alchemy |
| 28 | **贪婪之壶** | whole_word | `long_word_gold` | multi_adds_value | 5 | 9 | 单词 ≥6 字母且本轮首次打出该长度，倍率 +5 并获得 2 金币 | alchemy |
| 29 | **贤者之石** | limit | `philosopher` | limit | 1 | 12 | 限 1 次：将当前回合所有剩余金币转化为分数（1 金币 = 15 分） | alchemy |
| 30 | **药水共鸣** | flat_bonus | `potion_synergy` | — | 25 | 7 | 每装备 1 瓶药水（不限是否使用），基础分 +25 | alchemy |

---

## 三、组合技能（Synergy）设计 —— 共 10 组

组合技能的实现建议在 `calcWordScore()` 的 `totalScore` 计算之后、返回之前，增加一段：

```js
function applySynergyEffects(jokers, result, gameState) {
  const activeNames = new Set((jokers || []).filter(j => j && !j._disabled).map(j => j.name));
  // ... 遍历组合规则
}
```

| 组合名 | 所需女巫牌 | 联动效果 | 实现方式 |
|--------|-----------|---------|---------|
| **暗影审判** | 末字之刃 + 镜像双子 + 消元术 | 若同时装备，「末字之刃」额外追加末字母分数再算一次（即末字母分数 ×2 后再 +40） | 检测 activeNames，修改 cardAddScores[lastIdx] |
| **赌徒狂欢** | 命运硬币 + 孤注一掷 + 以小博大 | 三张同时装备时，「命运硬币」的负面效果（50% 不变）改为 70% 概率 ×2 | 在命运硬币 trigger 判断前检测组合，提升概率阈值 |
| **自然风暴** | 元音强化 + 辅音堡垒 + 风暴之眼 | 若同时装备，「风暴之眼」限用次数 +1，且触发时所有字母分数先 ×1.5 再算倍率 | 检测组合，修改 limit 和 letterGod 类似逻辑 |
| **时间闭环** | 首字连击 + 历史回响 + 轮回印记 | 同时装备时，「首字连击」每次叠加值从 +3 提升到 +5 | 在首字连击 value 累加处检测组合，+5 替代 +3 |
| **炼金帝国** | 点金术 + 贤者之石 + 药水共鸣 | 同时装备时，「贤者之石」转化率从 1:15 提升到 1:25，且使用后获得 1 瓶随机药水 | 在贤者之石触发处检测组合 |
| **生死一线** | 容错咒文 + 勇敢试错 + 生命延续 | 同时装备时，「勇敢试错」的 illegal_boost 每次 +2（而非 +1），且「生命延续」的惩罚目标分差值 ×1.5（而非 ×2） | 分别在 illegal_boost 和 life_extension 处检测 |
| **首尾联盟** | 元音为首 + 末字之刃 + 首尾呼应 | 同时装备时，若单词首尾均为元音，整词基础分额外 +30 | 在 flat_bonus 阶段追加判断 |
| **珍稀猎手** | 珍稀之力 + 暗影X + 庄家通吃 | 同时装备时，单词含 X 且含任意另一张人头牌，「珍稀之力」倍率 +5 改为倍率 +10 | 在珍稀之力 matched 后检测 |
| **潮汐共鸣** | 字母潮汐 + 元音潮汐 + 火山爆发 | 同时装备时，偶数位置字母若为元音则分数 +50（替代+25），若为 T 则分数 +60（替代+35） | 在 per_card 循环中检测组合并提升 add 值 |
| **被遗忘者** | 遗忘咒 + 首字连击 + 历史回响 | 同时装备时，若打出单词与上回合首字母不同且与上回合单词不同，基础分 +40（遗忘咒提升），且「首字连击」不会因为本次不同而重置 | 修改首字连击重置逻辑和遗忘咒 value |

---

## 四、代码植入建议

### 4.1 新增 trigger 判断函数

在 `js/game.js` 的 `_matchWordTrigger` 和 `_matchCardTrigger` 中追加：

```js
// _matchCardTrigger 新增
function _matchCardTrigger(c, trigger, idx, cards) {
  switch (trigger) {
    // ... 原有逻辑 ...
    case 'last_letter': return idx === (cards.length - 1);
    case 'letter_x': return c.letter === 'X';
    case 'letter_t': return c.letter === 'T';
    case 'even_position': return idx % 2 === 1; // 0-based，所以 idx=1 是第2位
    case 'is_consonant': return !['A','E','I','O','U'].includes(c.letter);
    case 'upgraded_card': return c.upgraded === true;
    default: return false;
  }
}

// _matchWordTrigger 新增
function _matchWordTrigger(cards, trigger, gameState) {
  const word = cards.map(c => c.letter.toLowerCase()).join('');
  switch (trigger) {
    // ... 原有逻辑 ...
    case 'middle_vowel': {
      if (word.length < 3) return false;
      const middle = word.slice(1, -1);
      return /[aeiou]/.test(middle);
    }
    case 'mirror_pair': {
      const len = word.length;
      if (len < 3) return false;
      for (let i = 0; i < Math.floor(len / 2); i++) {
        if (word[i] === word[len - 1 - i]) return true;
      }
      return false;
    }
    case 'no_duplicate': {
      const set = new Set(word.split(''));
      return set.size === word.length;
    }
    case 'all_vowel': {
      if (word.length < 3) return false;
      return word.split('').every(ch => 'aeiou'.includes(ch));
    }
    case 'has_a_and_e': return word.includes('a') && word.includes('e');
    case 'length_7': return cards.length === 7;
    case 'length_8_plus': return cards.length >= 8;
    case 'repeat_last_word':
      return gameState && gameState._lastPlayedWord === word;
    case 'haste_bonus':
      return gameState && gameState._hastePlayActive && gameState._hastePlayRemaining < 10;
    case 'same_length_streak':
      return gameState && gameState._lengthStreak >= 3;
    case 'long_word_gold':
      return cards.length >= 6;
    case 'gold_word':
      // 需要 result.base * result.mult >= 100，建议放在 calcWordScore 结果计算后判断
      return true; // 实际判断移至 post-check
    default: return false;
  }
}
```

### 4.2 新增 `comboGroup` / `comboWith` 字段

在 `js/shop.js` 的 `SHOP_POOL.witch` 数组末尾追加 30 张新牌对象（完整定义见上文表格）。每张牌增加 `comboGroup` 字段，例如：

```js
{name:'末字之刃', type:'witch', scope:'per_card', trigger:'last_letter', operation:'add', value:40, cost:5, desc:'单词最后一个字母分数 +40', comboGroup:'shadow_letter'},
```

### 4.3 组合技能检测入口

在 `js/game.js` 的 `calcWordScore` 返回 `totalScore` 之前，插入：

```js
  // === 组合技能（Synergy）检测 ===
  const synergyResult = applySynergyEffects(activeJokers, {score: totalScore, base: baseScore, mult}, gameState);
  if (synergyResult) {
    return { valid: true, score: synergyResult.score, base: synergyResult.base, mult: synergyResult.mult, word, hasFace, _lastLetterDouble: lastLetterDoubleExtra, _synergy: synergyResult.name };
  }

  return { valid: true, score: totalScore, base: baseScore, mult, word, hasFace, _lastLetterDouble: lastLetterDoubleExtra };
```

`applySynergyEffects` 建议单独写在 `js/witch_skills.js` 或 `js/game.js` 底部，保持主逻辑清晰。

### 4.4 状态持久化字段（可选）

部分新牌需要跨出牌/跨回合状态，建议在 `Game` 类中新增：

```js
this._lastPlayedWord = '';        // 历史回响
this._lengthStreak = 0;           // 轮回印记
this._lastWordLength = 0;         // 轮回印记辅助
this._predictedLetter = null;     // 预言家
this._discardCountThisRound = 0;  // 逆转翻盘 / 赌注翻倍
this._synergyTriggered = [];      // 本回合已触发的组合名（防重复）
```

在 `resetRound()` 中按需重置/保留。

---

## 五、平衡性说明

| 维度 | 说明 |
|------|------|
| **价格区间** | 新牌成本 5~12 金币，与现有牌（4~10）略有上浮，体现扩展包强度 |
| **组合难度** | 每组组合需要 3 张特定牌同时装备，在默认 4 格栏位中实现有策略取舍 |
| **limit 牌** | 仅「风暴之眼」「时间倒流」「贤者之石」为 limit 型，防止过度强大 |
| **概率牌** | 「命运硬币」「以小博大」等概率牌在组合后可提升稳定性，鼓励 build |
| **经济牌** | 「点金术」「贪婪之壶」等产出金币的牌强度中等，防止经济通胀 |

---

## 六、美术与命名建议

- **暗影字母系**：紫黑色调，匕首/镜面元素
- **赌徒之轮系**：金色/红色调，骰子/轮盘元素
- **自然共鸣系**：绿色/蓝色调，树叶/水流/火焰元素
- **时间旅者系**：银白色调，沙漏/齿轮/星座元素
- **炼金术士系**：黄铜色调，烧瓶/金币/药水元素

命名上采用四字中文名，保持与现有「元音强化」「珍稀之力」等一致的东方奇幻风格。

---

*设计文档基于现有代码框架（`per_card` / `whole_word` / `limit` / `flat_bonus` 四种 scope + operation 体系）整理，可直接按「四、代码植入建议」逐步落地。*
